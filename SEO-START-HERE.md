# WebShelf — نسخة تحسين الفهرسة

## الرفع

1. احتفظ بنسخة من مشروعك الحالي قبل الاستبدال.
2. انسخ محتويات فولدر WebShelf الموجود في الأرشيف فوق ملفات مشروعك الحالي، ووافق على استبدال الملفات المتطابقة. لا تحذف فولدر `.git` الخاص بمشروعك. الأرشيف لا يحتوي على تاريخ Git أو node_modules أو public.
3. تأكد أن الملفات الجديدة داخل `maintenance/templates/` وملفات `category-*.html` و`maintenance/seo.cjs` و`vercel.json` ستدخل الـCommit، وليس الملفات القديمة فقط.
4. اعمل Commit وPush بالطريقة المعتادة. لم يتم رفع هذه التعديلات للموقع بالنيابة عنك.
5. انتظر أن يظهر Deployment بحالة Ready. إذا ظهرت أخطاء، أرسل سجل الـbuild قبل تغيير أي إعداد.

## ما الذي تغير؟

- الرئيسية أصبحت تحتوي على روابط الأقسام وأول خمس مواقع من كل قسم قبل تشغيل JavaScript، مع تعريف واضح بـWebShelf.
- كل واحد من الأقسام الـ12 أصبح له HTML كامل يحتوي على قائمته وعنوانه ووصفه وcanonical خاص به.
- أُضيفت فقرة إرشادية مختلفة لكل قسم، وروابط للأقسام المرتبطة، وCollectionPage / BreadcrumbList / ItemList structured data مطابقة للقوائم.
- أُضيفت بيانات مشاركة اجتماعية لكل قسم.
- أصبحت روابط الأقسام صفحات مباشرة مثل `/anime-streaming.html`. روابط `category.html?type=...` القديمة تحصل على تحويل دائم للصفحة الجديدة، حتى لا نفقد أي إشارات سابقة.
- روابط TV القديمة بحروف كبيرة تحصل على نفس التحويل للصفحة الجديدة.
- أسماء ملفات التصدير الأول المتروكة مثل `/category-anime-streaming.html` تتحول أيضاً إلى الصفحة الصحيحة، فلا تسبب نسخة مكررة لو ظلت في Git.
- تم إصلاح رابط الرجوع المكسور `/l` في Suggest.
- تم فصل كاش صفحات الأقسام حسب query حتى لا يستبدل محتوى قسم بمحتوى قسم آخر.
- التوليد جزء من نظام الصيانة الحالي ويحدّث الصفحات عند تعديل قائمة المواقع؛ لن ترجع النسخة القديمة بعد build.

## مصادر التعديل مستقبلاً

| المطلوب | الملف الذي تعدله |
|---|---|
| إضافة أو إزالة مواقع | `maintenance/catalog.json` أو أدوات الصيانة الحالية |
| تصميم الرئيسية | `maintenance/templates/home.html` |
| قالب الصفحات الثابتة للأقسام | `maintenance/templates/category.html` |
| عنوان ووصف كل قسم | `CATEGORY_SEO` داخل `maintenance/src/category.js` |
| الفقرات الإرشادية وتوليد HTML | `maintenance/seo.cjs` |
| الألوان والتنسيق | `style.css` |
| كاش الأوفلاين | `maintenance/service-worker.source.js` و`maintenance/core.json` |

لا تعدل `index.html` أو صفحات الأقسام مثل `anime-streaming.html` أو ملفات bundle المتولدة مباشرة؛ سيعيد الـbuild كتابتها.

من فولدر المشروع الرئيسي، بعد تعديل المصدر:

```sh
npm ci --prefix maintenance --ignore-scripts
node maintenance/build.cjs
node maintenance/validate.cjs
npm test --prefix maintenance
```

صفحات الأقسام المسماة هي الروابط العامة التي تُنشر وتُضاف للـsitemap. الملف العام `category.html` متروك للتوافق مع الروابط القديمة والفحص المحلي.

## الاختبارات

- اجتاز المشروع فحص الملفات والمراجع والصور وصحة JavaScript والتوليد والـsitemap.
- اختبارات المشروع الأصلية: 83 اختباراً، بالإضافة إلى 15 اختباراً للفهرسة الثابتة والتوليد وقواعد التوجيه.
- نجح تصدير ملفات الإنتاج فقط إلى public في اختبار محلي.
- لا يوجد ضمان للظهور الأول أو للفهرسة. هذه تحسينات تقنية ومحتوى؛ سلطة الموقع وجودة الترشيحات والمنافسة عوامل أخرى.
- تعذر إجراء اختبار بصري بمتصفح: محرك المتصفح غير مثبت وتنزيله تعطل بمهلة اتصال. اختبارات قواعد Vercel محلية؛ لم ننشر deployment جديداً لاختبار تنفيذها على Vercel.
- فحص الصور هنا يتأكد من ملفاتها ومراجعها، وليس من عمل كل موقع خارجي أو حقوق المحتوى الذي يقدمه.

## فحص سريع بعد الرفع

1. افتح الرئيسية واختبر البحث والثيم والمفضلة وروابط الأقسام من الكمبيوتر والموبايل.
2. افتح `/anime-streaming.html`، ثم اعرض مصدر الصفحة (Ctrl+U). يجب أن تجد عنوان Anime Streaming وأسماء المواقع وcanonical الصحيح في المصدر نفسه.
3. كرر ذلك مع قسم آخر مثل `/tv-database.html` وتأكد أنه لا يعرض قائمة الأنمي.
4. افتح `/category.html?type=TV-streaming`؛ يجب أن يتحول إلى `/tv-streaming.html`.
5. افتح `/index.html`؛ يجب أن يتحول إلى `/`، وجرّب رابطاً غير موجود للتأكد من صفحة 404.
6. تأكد من ظهور الـ12 قسم في `/sitemap.xml` بنفس الحروف الصغيرة.

## Google Search Console

1. في Sitemaps تأكد من نجاح قراءة `https://www.webshelf.link/sitemap.xml`.
2. افحص الرئيسية ورابط أحد الأقسام عبر URL Inspection ثم Test Live URL. راجع HTML المعروض والـcanonical، ثم Request Indexing إذا كان الفحص سليماً.
3. عند تقييم صفحة مفهرسة، قارن User-declared canonical وGoogle-selected canonical. لا تفترض أن ظهور Duplicate يعني دائماً خطأ؛ قد يكون عنواناً بديلاً مقصوداً.
4. راقب Queries لكلمة `webshelf`، ثم كلمات الأقسام. راقب Impressions وClicks إلى جانب متوسط الترتيب. البيانات تتأخر ولا تتغير فوراً بعد الرفع.
5. لا تكرر طلب الفهرسة يومياً، ولا تشترِ backlinks أو زيارات وهمية. حسّن الترشيحات والوصف وشارك الموقع في سياقات مفيدة بدون spam.

`Discovered – currently not indexed` لا يثبت أن JavaScript سبب المشكلة؛ يعني أن Google يعرف الرابط ولم يزحف إليه وفق التقرير. Google قادر على تنفيذ JavaScript. كذلك تقارير Search Console وحدها لا تثبت أن العناوين المتشابهة هي سبب الاستبعاد.

مرجع رسمي: https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics
